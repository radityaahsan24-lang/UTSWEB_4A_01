// Memastikan semua elemen DOM sudah dimuat sebelum JS dieksekusi
document.addEventListener("DOMContentLoaded", () => {
    console.log("Bale Coffee Sanctuary Website - JavaScript Ready!");
    
    // Tempat kodingan fitur interaktif (Dark Mode / Filter Menu) nanti di sini
});